<% if !@update_marka.blank? %>
       <%= collection_select :filter, :marka, @update_marka, :id, :name, :prompt => 'Выберите марку' %>
    <% else %>
    <script type="text/javascript">
     	$('#update_marka_id').html("<select id=netdatem><option></option></select>");
     	$('#netdatem').attr("disabled", "disable");
     </script>
    <% end %>

	    
